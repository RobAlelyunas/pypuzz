from setuptools import setup

setup(name='pypuzz',
      version='0.1',
      description='puzzle rest services',
      url='',
      author='',
      author_email='',
      license='',
      packages=['pypuzz'],
      install_requires=[
          'flask-restful',
      ],
      zip_safe=False)